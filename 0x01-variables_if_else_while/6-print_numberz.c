#include <stdio.h>
/**
 * main - Print the digit numbers of base 10.
 * Return: Always 0 (succed).
 */
int main(void)
{
	int n = 48;

	while (n <= 57)
	{
		putchar(n++);
	}
	putchar('\n');
	return (0);
}
